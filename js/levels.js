class Level {
  constructor(level, enemyRadius, enemyColor, enemyAmount) {
    this.level = level
    this.enemyRadius = enemyRadius
    this.enemyColor = enemyColor
    this.enemyAmount = enemyAmount
  }

  level
}
